# Local development uses Vite's proxy and can leave this blank.
# In production (Netlify), set this to your deployed backend URL with /api on the end.
# Example: VITE_API_URL=https://talle-furniture-mart.onrender.com/api

VITE_API_URL=
